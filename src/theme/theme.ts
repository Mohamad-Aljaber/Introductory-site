import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2D0A31",
    },
    secondary: {
      main: "#4c1e51",
    },
    text: {
      primary: "#F067FF",
    },
  },
});

export default theme;
